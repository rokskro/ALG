@model CipherViewModel
<h5><i class="bi bi-braces"></i> Testing Ground</h5>
<p>Algorithim type: Symmetrical </p>

<div class="d-flex justify-content-center">
    <div class="card" style="width: 40rem;">
        <div class="card-body">   
            @using (Html.BeginForm("AESEncrypt", "Cipher", FormMethod.Post))
            {
            <div class="container">
                <h5 class="card-title"> AES Algorithim</h5>
                <div class="row">
                    <div class="col">
                        <div class="input-group mb-3">
                            <span class="input-group-text" id="key"><i class="bi bi-key"> 16 Digit Key</i></span>
                            @Html.TextBoxFor(model => model.EncryptionKey, new { @class = "form-control" })
                            <button type="submit" class="btn btn-dark btn-sm"><i class="bi bi-play"></i> Encrypt</button>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col">
                        <div class="card">
                            <div class="card-header bg-dark" style="color: #ffffff">
                                Text to Encrypt
                            </div>
                             <div class="card-body">
                                <div class="form-group">
                                    @Html.TextBoxFor(model => model.InputText, new { @class = "form-control" })
                                </div>   
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <div class="card">
                            <div class="card-header bg-dark" style="color: #ffffff">
                                Encrypted Text
                            </div>
                            <div class="card-body">
                                @if (!string.IsNullOrEmpty(Model.EncryptedText))
                                {
                                    <h6>Encrypted Text: @Model.EncryptedText</h6>
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>   
            }
        </div>
    </div>
</div>

<div class="card" style="width: 40rem;">
        <div class="card-body">   
            @using (Html.BeginForm("AESDecrypt", "Cipher", FormMethod.Post))
            {
            <div class="container">
                <h5 class="card-title"> AES Algorithim</h5>
                <div class="row">
                    <div class="col">
                        <div class="input-group mb-3">
                            <span class="input-group-text" id="key"><i class="bi bi-key"> 16 Digit Key</i></span>
                            @Html.TextBoxFor(model => model.EncryptionKey, new { @class = "form-control" })
                            <button type="submit" class="btn btn-dark btn-sm"><i class="bi bi-play"></i> Decrypt</button>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col">
                        <div class="card">
                            <div class="card-header bg-dark" style="color: #ffffff">
                                Text to Decrypt
                            </div>
                             <div class="card-body">
                                <div class="form-group">
                                    @Html.TextBoxFor(model => model.InputText, new { @class = "form-control" })
                                </div>   
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <div class="card">
                            <div class="card-header bg-dark" style="color: #ffffff">
                                Decrypted Text
                            </div>
                            <div class="card-body">
                                @if (!string.IsNullOrEmpty(Model.DecryptedText))
                                {
                                    <h6>Encrypted Text: @Model.DecryptedText</h6>
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>   
            }
        </div>
    </div>
